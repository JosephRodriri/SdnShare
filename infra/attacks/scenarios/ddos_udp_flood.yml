# DDoS UDP Flood
name: ddos_udp_flood
description: "Ataque DDoS UDP volumétrico: cada atacante debe superar 20000 pps"
label: ATTACK

target:
  host: h4
  ip: 10.1.1.4

attackers:
  - host: h1
  - host: h2
  - host: h3

tool: hping3
params:
  flags: "--flood --udp"
  port: 53
  duration_seconds: 60

# Uso manual en Mininet CLI:
# Cada fuente se evalúa de forma independiente por el detector FlowStats.
# h1 hping3 --flood --udp -p 53 10.1.1.4 &
# h2 hping3 --flood --udp -p 53 10.1.1.4 &
# h3 hping3 --flood --udp -p 53 10.1.1.4 &
